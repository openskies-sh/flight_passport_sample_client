version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    # environment:
    #   POSTGRES_HOST: db
    #   POSTGRES_PORT: 5432
    #   POSTGRES_USER: postgres
    #   POSTGRES_PASSWORD: pass
    #   POSTGRES_DB: service-finder
